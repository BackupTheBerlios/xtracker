java.util.Properties env = System.getProperties();
java.util.Enumeration en = env.propertyNames();
while(en.hasMoreElements()){
	String key =(String)en.nextElement();
	System.out.println(key + "=" + (String)env.get(key));
}